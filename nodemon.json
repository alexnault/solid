{
  "ignore": [".git", "node_modules", "**/*.spec.ts"],
  "watch": ["src"],
  "ext": "js,json,ts",
  "execMap": {
    "ts": "node --inspect -r ts-node/register/transpile-only -r tsconfig-paths/register"
  }
}
